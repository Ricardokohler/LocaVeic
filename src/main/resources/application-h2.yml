spring:

  datasource:
    url: jdbc:h2:file:./data/locaveics
    driver-class-name: org.h2.Driver
    username: dev
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true

  flyway:
    enabled: false